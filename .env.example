# Gmail SMTP Configuration
EMAIL_SERVER=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password  # Generate from Google Account > Security > App Passwords
EMAIL_FROM=support@yourdomain.com  # This is the "from" address users will see
ADMIN_EMAIL=your-admin@email.com  # Where notifications go
